// Syntax Error
const express = require('express'
const app = express(); // Missing closing parenthesis

// Debugging: Ensure all syntax is correct
const express = require('express');
const app = express(); // Fixed: Added closing parenthesis

// Syntax Error
function greet(name) {
    console.log("Hello, " + name); // Missing closing parenthesis
}

// Debugging: Ensure all syntax is correct
function greet(name) {
    console.log("Hello, " + name); // Fixed: Added closing parenthesis
}
